# Dia 3 · Manhã · front (M13)

Fala com o **mesmo** `InssApi` do Dia 2 (agora em `..\back` com CORS).

```bash
npm install
npm run dev
```

`http://localhost:5173` — Visual Studio precisa estar com a API em `5088`.
